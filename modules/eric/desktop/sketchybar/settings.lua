return {
    inside_background_padding = 7,
    between_icon_label_padding = 3,
    outer_padding = 3,
    ignored_apps = { "Raycast", "Finder", "Dropover", "CleanShot X" },
    mode_colors = {
        default = colors.green,
        zen = colors.blue,
        menu = colors.text,
    },
}
