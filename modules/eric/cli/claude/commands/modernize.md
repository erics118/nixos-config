---
description: Alias for /audit with the modernization lens
argument-hint: [empty for entire repo | area | "diff" for uncommitted changes]
---

Alias for the /audit command with the lens fixed to modernization. Invoke the audit skill
and run its modernization (idioms) lens only, passing $ARGUMENTS through as the scope (an
area, "diff" for the uncommitted changes, or empty for the whole repo).
