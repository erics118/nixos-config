---
description: Alias for /audit with the debt lens
argument-hint: [empty for entire repo | area | "diff" for uncommitted changes]
---

Alias for the /audit command with the lens fixed to debt. Invoke the audit skill and run
its debt lens only, passing $ARGUMENTS through as the scope (an area, "diff" for the
uncommitted changes, or empty for the whole repo).
